# MK Shop Server

1. Install npm packages:

```bash
npm install
```

2. Create a _.env_ file:

```bash
DB_USER=your_db_username
DB_PASS=your_db_password
```

3. Run the server

```bash
nodemon index.js
```
